// learn custom datatype in typescript
// 1. create a custom datatype
type CustomDataType = string | number;
let customData: CustomDataType = 10;
console.log(customData);
let customData2: CustomDataType = "Hello";
console.log(customData2);

// 2. create a function that uses the custom datatype
function f1() : CustomDataType {
    return 10;
}
console.log(f1());

// 3. create an object of the custom datatype
// 4. create an array of the custom datatype
// 5. create a class that uses the custom datatype
// 6. create an object of the class that uses the custom datatype
// 7. create an array of the class that uses the custom datatype
// 8. create a function that uses the class that uses the custom datatype
